name: First Workflow
on: push
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
          
        name: My Step
        run: echo "Hello, World!"